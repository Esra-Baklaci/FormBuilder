@model DynamicFormBuilder.Business.Models.FormDetailDto
@{
    ViewData["Title"] = "Teşekkürler";
    Layout = "_PublicLayout";
}

<div class="public-form-card text-center py-5">
    <div class="display-1 text-success mb-3"><i class="bi bi-check-circle"></i></div>
    <h1 class="h3">Teşekkürler!</h1>
    <p class="text-muted">@Model.Title formunuz başarıyla gönderildi.</p>
</div>
